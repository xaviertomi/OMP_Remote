"""Canonical OMP Remote application version used by the server and update API."""

# Android Gradle build reads these values directly; keep this source canonical.
VERSION_CODE = 5
VERSION_NAME = "0.5"
