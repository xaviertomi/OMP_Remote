# Keep this metadata aligned with server/omp_remote/version.py.
[project]
name = "omp-remote-server"
version = "0.2.0"
description = "Authenticated local API foundation for OMP Remote"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
